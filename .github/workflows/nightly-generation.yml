name: nightly-generation

on:
  workflow_dispatch:
  schedule:
    - cron: '0 12 * * *'

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Add model invocation or local orchestration here."
